S

- Csvを一行ずつ読み込ませ配列に入れていく
    ```
        string[] lines = File.ReadAllLines(filePath);
    ```

- 上記の配列内に入っているcsvの列データの個数を調べる
    ```
       int colCount = lines[0].Split(',').Length;
    ```
- これ以降は適当にパァ〜とおできると思う